services:
  lich-server:
    build:
      context: ./lich-server
      dockerfile: Dockerfile
    image: lich-server:latest
    container_name: lich-server
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.lich-server.rule=Host(`${DOMAIN:?DOMAIN is required in .env}`)"
      - "traefik.http.routers.lich-server.entrypoints=websecure"
      - "traefik.http.routers.lich-server.tls.certresolver=myresolver"
      - "traefik.http.services.lich-server.loadbalancer.server.port=3000"
    volumes:
      - ./data:/data
    env_file:
      - .env
    environment:
      TZ: "Asia/Ho_Chi_Minh"
      TRUST_PROXY: true
    networks:
      - traefik_default
    restart: unless-stopped

networks:
  traefik_default:
    external: true
